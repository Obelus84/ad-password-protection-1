// TestFuncs.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <vector>
#include <fstream>
#include <Shlwapi.h>
#include <mutex>
#include <ctime>
#include <iomanip>
#include <iostream>



int main()
{
	
	/*std::time_t t = std::time(nullptr);
	std::tm tm;
	localtime_s(&tm, &t);

	std::wstring format = L"%Y-%m-%d %H:%M:%S";
	wchar_t buffer[100];
	std::wcsftime(buffer, 100, format.c_str(), &tm);

	std::wstring datetime(buffer);
std::wcout << datetime << std::endl;

delete[] buffer;

	return 0;*/
	
}

